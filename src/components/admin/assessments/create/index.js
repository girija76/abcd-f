import React from 'react';
import AssessmentEditor from '../editor';
import Editor from '../editor';

const CreateAssessment = () => {
	return (
		<>
			<AssessmentEditor kind={'create'} />
		</>
	);
};

export default CreateAssessment;
